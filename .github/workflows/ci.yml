name: CI

on:
    push:
        branches: [main]
    pull_request:
        branches: [main]

permissions:
    contents: read

concurrency:
    group: ${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true

jobs:
    test:
        name: Test (${{ matrix.os }})
        runs-on: ${{ matrix.os }}
        strategy:
            fail-fast: false
            matrix:
                os: [ubuntu-latest, macos-latest]
        steps:
            - uses: actions/checkout@v6
            - name: Setup Bun
              uses: oven-sh/setup-bun@v2
              with:
                  bun-version: latest
            - name: Install
              run: bun install --frozen-lockfile
            - name: Format check
              run: bun run format:check
            - name: Lint
              run: bun run lint
            - name: Typecheck
              run: bun run typecheck
            - name: Dead-code check
              run: bun run deadcode
            - name: Test
              run: bun run test
            - name: Build
              run: bun run build
            - name: Size budget
              run: bun run size

    lint-docs:
        name: Lint docs
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v6
            - name: Setup Bun
              uses: oven-sh/setup-bun@v2
              with:
                  bun-version: latest
            - name: Install markdownlint
              run: bun add -g markdownlint-cli2
            - name: Markdown lint
              run: markdownlint-cli2 '**/*.md' '#node_modules' '#dist' '#coverage' '#.opencode'
            - name: Link check
              uses: lycheeverse/lychee-action@v2
              with:
                  args: --config lychee.toml '**/*.md'
                  fail: true

    coverage:
        name: Coverage
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v6
            - name: Setup Bun
              uses: oven-sh/setup-bun@v2
              with:
                  bun-version: latest
            - name: Install
              run: bun install --frozen-lockfile
            - name: Coverage
              run: bun run test:coverage
            - name: Upload coverage artifact
              uses: actions/upload-artifact@v4
              with:
                  name: coverage
                  path: coverage/
                  retention-days: 14
